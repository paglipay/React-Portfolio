import React from 'react'
// import { Card, Form, Button, Container, Row, Col } from 'react-bootstrap';

function UrlWrapper(props) {
    return (
        <>            
            {props.children}
        </>
    )
}

export default UrlWrapper
